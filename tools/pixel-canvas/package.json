{
  "name": "pixel-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "wasm-guest"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "compile": "npm run compile -w wasm-guest",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w wasm-guest"
  }
}
